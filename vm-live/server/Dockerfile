# Liten container för push-backenden. Fungerar på Render, Railway, Fly.io, m.fl.
FROM node:20-slim
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install --omit=dev
COPY src ./src
ENV PORT=4000
EXPOSE 4000
CMD ["node", "src/index.js"]
